# Natural Log Transformed, Cubic fit data

X = calDataTable[,arCol]
Y = calDataTable[,concCol]
# plot our data in R
# plot(X, log(Y+1), col="black", main="CRP panel data lot RDP3")

# performs cubic fit on log-transformed data
fit1 = lm(log(Y+1) ~ X + I(X^2) + I(X^3))
# gathers coefficients from fitting
coefs = coef(summary(fit1))[,"Estimate"]

# Adds line to plot
# lines(X, predict(fit1, data.frame(x=X)), col='blue')